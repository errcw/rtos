/*
 * Kernel limits.
 */
#ifndef __LIMITS_H
#define __LIMITS_H

/* The maximum number of processes. */
#define PROCESS_MAX (128)

/* The minimum priority. */
#define PRIORITY_MIN (0)
/* The maximum priority. */
#define PRIORITY_MAX (31)

/* The maximum number of registerable process names. */
#define NAME_MAX (128)
/* The maximum process name length. */
#define NAME_MAXLEN (128)

#endif

