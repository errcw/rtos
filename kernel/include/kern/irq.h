/*
 * IRQ numbers for devices. Useful for AwaitEvent(irq).
 */
#ifndef __IRQ_H
#define __IRQ_H

#define IRQ_TIMER (0)
#define IRQ_KEYBOARD (1)
#define IRQ_SERIAL1 (3) /* terminal */
#define IRQ_SERIAL0 (4) /* train */
#define IRQ_SOUND (5)
#define IRQ_RTC (8)

#endif

